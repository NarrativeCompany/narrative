import { defineMessages } from 'react-intl';

export const NetworkErrorMessages = defineMessages({
  ServerUnreachable: {
    id: 'networkError.serverUnreachable',
    defaultMessage: 'The server is currently unreachable'
  }
});
